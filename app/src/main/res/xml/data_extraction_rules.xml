<?xml version="1.0" encoding="utf-8"?>
<!--
  Keep the persisted settings (theme, market selection, sort, extended mode) out of
  cloud backup and device-to-device transfer so a fresh install always starts from
  the in-code defaults (e.g. extended mode off) instead of restoring an old state.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore" />
    </device-transfer>
</data-extraction-rules>
